// Sprint 1: Core typing logic
// - Character-by-character input tracking
// - WPM calculation (standard: words = characters / 5)
// - Accuracy calculation (correct / total keystrokes)
// - Error detection (wrong key, position tracking)
// - Timer (countdown for speed test, count-up for lessons)
